
    <main>
        <div>
            <h1>{{{character}}}</h1>
            <img alt={{{character}}} src={{{image}}}>
            <p>{{{description}}}</p>
            <h2>Comics</h2>
            <ul>
                {{#each comics}}
                <li>{{{name}}}</a></li>
                {{/each}}
            </ul>
        </div>
        <a href="/">Make another search</a>
    </main>
